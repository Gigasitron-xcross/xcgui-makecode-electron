XC_GUI PNG to TypeScript Converter

Requirements:
- Python 3
- Pillow

Install Pillow:
pip install pillow

Example:
python png_to_xcgui_ts.py clock_face.png -o clock_face.ts

Default output format:
RLE compressed XC_GUI bitmap.

The generated .ts file can be added to your MakeCode project.